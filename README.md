# 神州租车全国搜车工具

一个基于神州租车（zuche.com）H5 网关接口的桌面小工具，用于**一键扫描全国所有开放城市**的车辆库存与价格，并支持按车型筛选、表格查看、全国地图可视化和 CSV 导出。

> 本工具通过复用你本人登录后的浏览器 Cookie 调用神州租车 H5 站点的公开接口，仅用于个人查价比价，不涉及任何账号密码或支付信息。请遵守神州租车官网的服务条款，合理控制请求频率，仅供学习交流使用。

## 功能特性

- 🔍 **全国扫描**：输入 Cookie、起租日期与租期天数，自动遍历全部开放城市，抓取各网点车型与价格
- 📊 **结果查询**：按车型多选筛选，表格展示城市 / 网点 / 地址 / 配置 / 日租 / 套餐价 / 预估总价，并自动排序取最低价
- 🗺️ **地图可视化**：基于 ECharts 生成中国地图热力散点图，直观查看各城市价格分布
- 📥 **CSV 导出**：导出筛选后的结果表格，方便进一步分析
- 💾 **本地缓存**：自动缓存 Cookie 与上次扫描结果，下次打开自动加载
- 🚦 **限速与重试**：内置请求频率限制与失败自动重试，降低触发风控概率

## 环境要求

- Windows 系统
- Python 3.8+（需自带 `tkinter`，官方安装包默认包含）
- 依赖库：`requests`（见 `requirements.txt`）

## 快速开始

### 方式一：双击启动脚本（推荐）

直接双击 [`start.bat`](start.bat)，脚本会自动检查 Python 环境、安装缺失依赖并启动程序。

### 方式二：命令行启动

```bash
pip install -r requirements.txt
python main.py
```

## 使用说明

1. 获取登录 Cookie（见下方「[如何获取 / 更新 Cookie](#如何获取--更新-cookie)」）
2. 将 Cookie 粘贴到程序「全国扫描」页的输入框中
3. 按需设置代理、起始日期、租期天数
4. 点击「🚀 全国扫描」，等待扫描完成（可随时点击「⏹ 停止」中断）
5. 切换到「📊 结果查询」页，在左侧车型列表中多选车型（按住 Ctrl 多选），点击「📊 查看表格」或「🗺️ 显示地图」查看结果，也可点击「📥 导出CSV」导出数据

## 如何获取 / 更新 Cookie

程序需要一个包含 `intranet-sessionid` 的登录态 Cookie 才能调用神州租车的接口。获取方式（以 Chrome 为例）：

1. **打开移动版页面**：浏览器访问 `https://m.zuche.com`。建议按 `F12` 打开开发者工具后，点击左上角的「切换设备工具栏」图标（或按 `Ctrl+Shift+M`）切换成手机视图，再刷新页面，确保加载的是 H5 移动版而不是桌面版
2. **登录账号**：按提示用手机号 + 验证码登录你自己的神州租车账号
3. **抓取请求**：切到开发者工具的 **Network（网络）** 面板，勾选 `Fetch/XHR` 过滤，随便在页面上操作一下（比如切换城市、点一下选车），列表中会出现请求地址包含 `gw.do` 的接口
4. **复制 Cookie**：点击任意一个 `gw.do` 请求 → 右侧 **Headers** → 找到 **Request Headers** 里的 `cookie:` 一行 → 把冒号后面的完整值全部复制（应包含 `intranet-sessionid=xxxx` 字段）
5. **粘贴到程序**：打开程序「🔍 全国扫描」页，把复制的内容整段粘贴进 Cookie 输入框（多粘贴一些没关系，程序会自动提取 `intranet-sessionid` 所在的那一行并清理格式），点击「🚀 全国扫描」

**什么时候需要更新 Cookie：**

- 扫描过程中程序会自动读取接口返回的新 `intranet-sessionid` 并写回 `cookie_cache.txt`（见 `main.py` 里的 `refresh_cookie`），所以正常扫描期间一般不需要手动更新
- 如果长时间没打开程序，或者扫描时看到日志里**几乎每个城市都报错**（例如提示登录失效、需要重新登录、返回内容为空等），说明会话已过期，需要按上面的步骤重新登录并复制一份新的 Cookie，粘贴替换即可（会自动覆盖 `cookie_cache.txt` 里的旧缓存）
- Cookie 本身包含账号会话信息，注意不要发给别人或提交到 Git 仓库（已在 `.gitignore` 中排除 `cookie_cache.txt`）

## 项目结构

```
.
├── main.py               # 主程序（Tkinter GUI + 扫描 / 查询 / 地图 / 导出）
├── start.bat              # 一键启动脚本
├── requirements.txt       # Python 依赖
├── city_fields.json       # 城市接口字段参考样例
├── check_city_api.py      # 调试：校验城市列表接口返回字段
├── check_coords.py        # 调试：查看扫描结果中的坐标数据
├── discover_api.py        # 调试：探测网关接口
├── find_api.py             # 调试：从前端 JS 中查找 API 端点
├── find_cities.py          # 调试：获取完整开放城市列表
├── read_result.py          # 调试：读取分析结果文件
└── test_api*.py / test_direct.py  # 调试：接口调用格式验证脚本
```

`check_*` / `discover_*` / `find_*` / `test_*` 系列脚本是逆向分析接口时使用的调试脚本，非运行主程序所必需，仅作为参考保留。

## 运行时生成 / 不纳入版本控制的文件

以下文件包含个人敏感信息或为运行时产物，已在 `.gitignore` 中排除，**不会**被提交到仓库：

- `cookie_cache.txt` —— 缓存的登录 Cookie（含会话 ID、手机号等隐私信息）
- `scan_data.json` —— 扫描结果缓存
- `map.html` —— 地图可视化生成结果
- `shenzhou_*.csv` —— 导出的表格数据

⚠️ **请勿将自己的 `cookie_cache.txt` 或导出的 CSV 分享给他人 / 提交到公开仓库**，其中可能包含账号会话信息。

## 免责声明

本项目仅用于个人学习与私人比价用途，请勿用于高频请求、商业用途或任何违反目标网站服务条款的行为。因使用本工具产生的任何后果由使用者自行承担。
